import { SectionProps } from "./Section.types";
import { SectionWrapper } from "./Section.styles";

export function Section({ }: SectionProps) {
    return (
        <SectionWrapper>

        </SectionWrapper>
    );
}